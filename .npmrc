# @types/node is pinned to ^20 but some transitive deps (vitest 5, @types/ws
# via @libsql) ask for newer. The tree resolves fine in practice; this keeps
# `npm install` from hard-failing on the peer conflict (local + Vercel).
legacy-peer-deps=true
